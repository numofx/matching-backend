[build]
builder = "RAILPACK"
buildCommand = "go build -o out ./cmd/api && go build -o migrate ./cmd/migrate"

[deploy]
preDeployCommand = "./migrate"
startCommand = "./out"
runtime = "V2"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
